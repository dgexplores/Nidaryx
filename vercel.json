{
  "buildCommand": "npm ci && npm run web:build",
  "outputDirectory": "apps/web-ui/dist",
  "installCommand": "npm ci",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

{
  "buildCommand": "npm run web:build",
  "outputDirectory": "dist",
  "installCommand": "npm ci --loglevel=error",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

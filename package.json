{
  "name": "tracesentry-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web-ui"
  ],
  "scripts": {
    "web:dev": "npm --workspace apps/web-ui run dev",
    "web:build": "npm --workspace apps/web-ui run build",
    "web:lint": "npm --workspace apps/web-ui run lint",
    "web:typecheck": "npm --workspace apps/web-ui run typecheck"
  }
}

